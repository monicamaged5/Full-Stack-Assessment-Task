{
  "name": "@projectflow/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@projectflow/eslint-config": "workspace:*",
    "@projectflow/tsconfig": "workspace:*",
    "eslint": "9.39.5",
    "typescript": "5.9.3"
  }
}
